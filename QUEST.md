# B5-1: 정보를 깔끔하게 정리하는 디지털 서랍장 만들기

## 과제 정보

| 항목 | 첨부 미션 기준 |
|---|---|
| 분야 | AI/SW 기초 |
| 구분 | 데이터베이스와 백엔드 |
| 학습 시간 | 40시간 |
| 기준 원문 | 2026-08-15 제공 PDF 6쪽 |

기존 저장소에 있던 난이도, 필수 여부, 과제 번호 `185012`는 첨부 PDF 본문에서는 확인되지 않으므로 별도 플랫폼 화면에서 확인할 항목으로 남긴다.

## 1. 미션 소개

백엔드 프레임워크 없이 SQL을 직접 사용해 다음 흐름을 완성한다.

```text
도메인 선택 → 관계형 테이블 설계 → 샘플 데이터 입력 → SQL로 요구사항 해결 → 결과 증거 보관
```

핵심은 데이터 양이 아니라 PK·FK·1:N 관계, 무결성, JOIN과 GROUP BY를 이해하는 것이다. 이후 ORM/JPA가 대신하는 일을 SQL 관점에서 먼저 경험한다.

## 2. 최종 결과물

1. 자유 주제 도메인 데이터베이스 1개
   - 테이블 최소 4개
   - 1:N 관계 최소 2개
2. 스키마 생성 SQL 1개
   - CREATE TABLE
   - PK, FK, 제약조건
   - 실행 순서대로 정리
3. 샘플 데이터 INSERT SQL 1개
   - 각 테이블 최소 10행
   - FK 관계가 실제로 연결된 데이터
4. 핵심 쿼리 SQL 1개와 결과 자료
   - 쿼리 15개 이상
   - 각 쿼리 한 줄 설명
   - 각 쿼리의 스크린샷 또는 결과 텍스트
5. 선택 결과물
   - ERD 다이어그램 이미지

## 3. 학습 목표

- 엑셀과 관계형 DB의 차이와 테이블을 나누는 이유를 설명한다.
- PK, FK, 1:N 관계를 설명한다.
- SELECT, INSERT, UPDATE, DELETE 용도를 구분한다.
- JOIN과 GROUP BY로 연결·집계 데이터를 조회한다.
- 검색, 정렬, 집계, 랭킹 요구를 SQL로 해결한다.
- 인덱스가 필요한 이유와 적용 후보 컬럼을 기초 수준에서 설명한다.

## 4. 기능 요구사항

### 4.1 DB 환경

- SQLite, MySQL, PostgreSQL, H2 중 로컬 실행 가능한 DB 1개 선택
- DBeaver, TablePlus, DataGrip, CLI 등 SQL 실행 도구 준비
- 가능한 표준 SQL을 사용한다.
- DB 전용 문법은 주석으로 DB 종류와 차이를 명시한다.

### 4.2 스키마

- 테이블 최소 4개
- 모든 테이블에 PK
- FK 최소 2개로 1:N 관계 최소 2개
- 역할에 맞는 타입과 이름 사용
- NOT NULL 최소 1개
- UNIQUE 최소 1개
- 존재하지 않는 부모를 참조하는 FK 입력이 실제로 차단되어야 함

### 4.3 샘플 데이터

- 각 테이블 최소 10행
- 부모 테이블을 먼저 입력한 뒤 자식 테이블 입력
- FK가 실제 관계를 갖도록 의미 있는 데이터 사용

### 4.4 핵심 SQL 15개 이상

| 범주 | 최소 수량·조건 |
|---|---|
| 기본 조회 | 4개 이상, WHERE·ORDER BY·LIMIT 포함 |
| JOIN | 4개 이상, INNER JOIN 2개 이상·LEFT JOIN 1개 이상 |
| 집계 | 3개 이상, COUNT·SUM·AVG 중 2개 이상과 GROUP BY |
| 서브쿼리 | 1개 이상 |
| 수정·삭제 | UPDATE·DELETE를 합쳐 2개 이상 |
| 인덱스 | CREATE INDEX 1개 이상과 적용 이유 1줄 |

### 4.5 결과 증거

- 각 쿼리가 무엇을 확인하는지 한 줄 설명
- 각 쿼리의 실행 결과를 이미지 또는 텍스트로 보관
- SQL과 결과 파일의 번호·설명·데이터가 서로 일치해야 함

## 5. 보너스 과제

1. 같은 요구를 JOIN과 서브쿼리 두 방식으로 해결하고 차이 비교
2. 일부러 FK 오류 입력을 시도하고 차단 이유·수정 방법 기록
3. 핵심 지표 3개를 정의하고 각 지표 SQL을 최종본으로 정리

## 6. 개발 환경

현재 프로젝트 선택:

- DB: SQLite 3
- 실행: sqlite3 CLI 또는 Python 표준 라이브러리 `sqlite3`
- ERD·결과 이미지: 검증 스크립트로 재생성

SQLite 전용 요소인 `PRAGMA foreign_keys`, `LIMIT`, 유연한 날짜 타입 처리는 SQL 주석과 README에 명시한다.

## 7. 제약사항

- Spring, Django, Express 등 백엔드 프레임워크로 API·화면을 만들지 않는다.
- 로컬 실행 DB만 사용한다.
- View, 프로시저, 트리거를 사용하지 않는다.
- 정규화 이론을 과도하게 확장하지 않고 관계가 자연스럽고 쿼리가 잘 나오는 구조를 목표로 한다.

## 8. 현재 프로젝트 주제

스마트 테이블 오더 시스템

- `menu_categories` 1:N `menus`
- `store_tables` 1:N `orders`
- `menus` 1:N `orders`

총 4개 테이블과 3개 1:N 관계를 사용한다.
