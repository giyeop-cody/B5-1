# [비전공 일반인을 위한 메가급 딥다이브 바이블] 순수 SQL로 만드는 백엔드 데이터베이스 완전 정복

> **집필 목적**: 생전 처음 컴퓨터 소프트웨어와 데이터베이스를 접하시는 비전공 일반인 입문자께서 외부 강의나 멘토의 도움 없이 단 한 권의 본 바이블만 정독하시더라도, ①데이터베이스의 물리적 원리 체득, ②추상적 비즈니스의 스키마 정규화 설계, ③최적 DBMS 선정, ④15개 핵심 비즈니스 쿼리 구사, ⑤트러블슈팅 방어까지 현업 시니어 엔지니어 동급 수준으로 완벽하게 스스로 도달해 내실 수 있도록 체계화한 대과외 교과서입니다.  
> **집필 톤앤매너**: 시니어 백엔드 및 데이터베이스 전문가가 입문자분들께 정중하고 사려 깊게 하나하나 풀어서 가르쳐드리는 격조 높은 설명 방식  
> **실습 도메인 주제**: 스마트 테이블 오더 관리 시스템 데이터베이스 (식당/주점 스마트 키오스크 실시간 주문 DB)

---

# 【제0부】 맨땅 오리엔테이션 : 컴퓨터는 데이터를 어떻게 기억하는가?

데이터베이스를 처음 접하실 때 가장 빈번하게 마주하시는 심리적 장벽은 컴퓨터 하드웨어와 소프트웨어가 데이터를 다루는 방식에 대한 생소함입니다. 본격적인 데이터 모델링과 SQL 구문 학습에 앞서, 컴퓨터 시스템의 물리적 그릇의 원리와 핵심 기초 개념 3가지를 정밀하게 규명하고 출발하겠습니다.

## 1. 하드디스크(SSD/HDD)와 메모리(RAM)의 물리적 매커니즘 차이
컴퓨터 시스템이 데이터를 보관하는 기억 장치는 물리적 특성과 데이터 생존 생명주기에 따라 크게 두 가지로 격리됩니다.

* **메모리 (RAM : 주기억장치)**: 인간이 일상적으로 업무를 수행하는 '책상 위 작업대'와 같습니다. CPU와 직접 통신하며 연산을 수행하므로 데이터 읽기 및 쓰기 속도가 빛의 속도에 가깝게 빠릅니다. 그러나 결정적인 한계점이 존재합니다. **컴퓨터의 전원을 종료하거나 애플리케이션 프로세스를 종료하는 순간, 책상 위에 올려두었던 모든 데이터가 공백으로 싹 소멸(증발)합니다.** 이를 컴퓨터 공학에서는 '휘발성(Volatility)'이라고 부릅니다.
* **하드디스크 (SSD 및 HDD : 보조기억장치)**: 책상 밑에 놓인 '철제 수장고 서랍장'과 같습니다. 데이터를 전기적 신호나 자기적 규격으로 고정하여 보관하므로 I/O 속도는 메모리 작업대보다 다소 느립니다. 그러나 **컴퓨터 전원이 차단되거나 프로그램이 강제 종료되더라도 데이터가 소멸하지 않고 영구적으로 보존됩니다.** 이를 '비휘발성(Non-volatility)'이라고 부릅니다.

우리가 설계하고자 하는 비즈니스 서비스(예: 은행의 잔고 정산 시스템, 전자상거래 쇼핑몰의 주문 결제 장부, 식당의 키오스크 매출 내역)에서 전원이 꺼졌다고 데이터가 증발한다면 비즈니스 자체가 붕괴됩니다. 따라서 **데이터베이스는 반드시 하드디스크라는 비휘발성 영구 수장고 안에 안전하게 규격화되어 거주해야 하는 핵심 백엔드 소프트웨어**입니다.

## 2. 일반 파일 시스템(엑셀, 텍스트) 방식의 치명적 한계점 5가지
입문자분들께서 흔히 품으시는 *"굳이 복잡한 데이터베이스를 배울 필요 없이, 그냥 하드디스크에 엑셀 파일(.xlsx)이나 메모장 텍스트 파일(.txt)을 만들어 데이터를 적으면 되지 않나요?"*라는 의문에 대해 전 세계 소프트웨어 엔지니어들이 데이터베이스 엔진을 도입할 수밖에 없는 5가지 결정적 한계 이유를 서술해 드립니다.

1. **동시 접근 제어의 한계 (Concurrency Limit)**: 금요일 저녁 피크타임 매장에서 15개 테이블의 손님이 동시에 키오스크 화면에서 '주문하기' 버튼을 터치한다고 가정해 봅시다. 일반 파일 시스템은 한 명의 프로세스가 파일을 열어 데이터를 기록하는 동안, 파일 전체에 물리적인 잠금(File Lock)이 걸려 다른 14명의 쓰기 요청이 거부되거나 대기열에서 에러로 차단됩니다.
2. **데이터 중복과 불일치성 (Redundancy & Inconsistency)**: 여러 시트나 텍스트 파일 곳곳에 동일한 손님의 연락처 정보를 중복해서 적어둘 경우, 손님이 전화번호를 변경했을 때 담당자가 한 곳만 수정하고 다른 곳을 누락하면 데이터 간에 엇갈림 현상이 발생하여 서비스 신뢰도가 훼손됩니다.
3. **타입 보호 및 데이터 규격화 기능의 부재**: 결제 금액이 기록되어야 하는 칸에 사용자의 키오스크 터치 오류로 문자열이나 공백이 전송되더라도, 일반 파일 시스템은 이를 사전에 검증하여 차단하는 능력이 없습니다.
4. **보안 및 접근 권한 통제의 취약성**: 파일 전체가 하나의 물리적 단위로 존재하므로, 아르바이트생이나 외부 프로그램이 파일 전체를 통째로 복사해 가거나 실수로 파일 자체를 삭제당할 위험에 무방비로 노출됩니다.
5. **대용량 데이터 스캔 속도 저하**: 데이터 레코드가 1,000만 건 이상 누적될 경우, 엑셀 파일은 본문을 메모리로 로드하는 데만 수분이 소요되어 애플리케이션의 실시간 조회 응답 속도가 치명적으로 저하됩니다.

---

## 3. 맨땅 기초 아키텍처 삼위일체 규명 : DB vs DBMS vs SQL

```text
[사용자가 타이핑하는 국제 표준 질의어]    [명령을 판독하여 창고에서 일해주는 사서 소프트웨어]    [실제 하드디스크에 보관된 규격 데이터]
              SQL 언어               ───►             DBMS 프로그램              ───►             Database (DB)
 (SELECT * FROM orders WHERE...)                       (SQLite, MySQL 등)                        (바이너리 표 집합체 원본)
```

* **① Database (DB : 데이터베이스)**: 하드디스크에 바이너리 표 포맷으로 구조화되어 영구 저장된 '데이터 레코드 원본 집합체'입니다.
* **② DBMS (Database Management System)**: 창고 입구에서 사용자의 명령을 받아 DB 원본을 메모리 위로 꺼내거나 주입해 주는 '진능형 전문 사서 관리 소프트웨어'입니다. *(우리가 컴퓨터에 설치하는 프로그램)*
* **③ SQL (Structured Query Language)**: 한국어나 영어를 모르는 관리인 소프트웨어(DBMS)에게 업무를 지시하기 위해 전 세계가 표준화한 '국제 표준 데이터 질의 규격 언어'입니다.

## 4. 과제 제시 4대 DBMS 결정적 스택 비교

| DBMS 명칭 | 소프트웨어 아키텍처 비유 | 핵심 기술적 강점 | 치명적 한계점 | 과제 적합도 |
|:---|:---|:---|:---|:---:|
| **SQLite**<br>(스큐엘라이트) | **경쾌한 개인용 모빌리티<br>(서버리스 내장 엔진)** | • 백그라운드 서버 프로세스 설치 불필요<br>• PC 폴더에 `.db` 파일 1개만 생성하면 그 파일이 즉시 DB이자 엔진이 됨<br>• 카톡 파일 전송하듯 복사/백업 가능 | • 다중 접속자가 동시 쓰기를 쏟아내는 대형 상업 웹사이트 호스팅에는 불리함 | **★ 입문 실습 1위**<br>(구축 소요시간 1분) |
| **MySQL**<br>(마이에스큐엘) | **글로벌 IT 업계 표준<br>국민 세단 자동차** | • 전 세계 백엔드 실무 점유율 1위 스택<br>• 대용량 다중 접속 트랜잭션 처리 탁월<br>• 한글 에러 해결 자료 최다 보유 | • PC에 서버를 깔고 네트워크 포트, 계정을 설정하는 초기 구축 난이도 존재 | **실무 취업 표준**<br>(구축 난이도 중상) |
| **PostgreSQL**<br>(포스트그레스) | **첨단 엔지니어링이 집약된<br>전기차 테슬라** | • 위치 좌표(GPS 연산), 고도화된 통계 분석 등 복잡한 연산에 특화된 학구파 DB<br>• 국제 표준 엄격 준수 및 보호능력 끝판왕 | • 초보자분들께서 입문하시기에 규격 규칙이 다소 엄격하게 느껴지실 수 있음 | **트렌디 스택**<br>(구축 난이도 중상) |
| **H2**<br>(에이치투) | **전원 종료 시 증발하는<br>가상 테스트 창고** | • Java 백엔드(Spring 프레임워크) 개발 시 중간 로직 검증용 내장 엔진<br>• RAM 위에서 구동되어 속도 극대화 | • **애플리케이션 종료 시 RAM에 보관되던 데이터가 공백으로 소멸됨** | **백엔드 중간검증용**<br>(단독 실습 불리) |

### 💡 베테랑 전문가의 제언: 본 실습 프로젝트에서 'SQLite'를 선정해야 하는 엔지니어링 배경
초보자께서 MySQL이나 PostgreSQL을 선택하시면 스키마 설계 본업에 들어가시기도 전에 로컬 서버 설정 단계에서 에러를 겪고 지쳐버립니다. 반면 **SQLite는 무료 그래픽 툴인 DBeaver를 켜신 후 `새 연결 -> SQLite`를 누르시고 파일명에 `table_order.db`라고 입력하시는 단 1분 만에 100% 독립 실습 환경 구축이 종결됩니다.**


# 【제1편】 과제 원본 명세서 1:1 정밀 판독 해설

첨부된 과제 명세서(PDF 1~6페이지)에 기재된 모든 원문 텍스트를 파트별로 전문 수록하고, 각 문장이 지니는 **백엔드 엔지니어링 관점의 진짜 기술적 의도**를 시니어 전문가가 하나씩 해석해 드립니다.

## 1. 미션 소개 및 최종 결과물 판독
> **[과제 원문 텍스트]**  
> *"엑셀과 DB의 차이는 '데이터가 얼마나 많냐'가 아닙니다. 테이블 사이의 '관계'를 표현할 수 있냐의 차이입니다. 그 관계를 모르면 JOIN도 ORM도 계속 헷갈립니다... 백엔드 프레임워크 없이, 도메인에 맞는 테이블 구조를 설계하고 직접 데이터를 넣고 필요한 정보를 뽑는 전체 흐름을 실습합니다... 최종 결과물: ①도메인 데이터베이스 1개 설계(최소 4개 테이블, 1:N 관계 2개 이상), ②스키마 생성 스크립트 1개, ③샘플 데이터 입력 스크립트 1개(테이블당 최소 10행), ④핵심 쿼리 15개 + 실행 결과 캡처"*

* **▶ 시니어 전문가의 명쾌한 해석**:
  * 이 과제는 애플리케이션 코딩(Python, Java 등)에 의존하지 않고 순수 SQL 스크립트만으로 SW 개발의 뼈대인 '데이터 계층'을 독립 구축하는 프로젝트입니다.
  * 백엔드 ORM(JPA, SQLAlchemy) 프레임워크가 뒤에서 자동으로 생성해 주는 SQL을 엔지니어가 투명하게 통제할 수 있도록, **테이블을 나누고(정규화) 외래키(FK)로 연결하는 본질적 사고방식을 굳히는 미션**입니다.

## 2. 과제 목표 및 기능 요구사항 판독
> **[과제 원문 텍스트]**  
> *"과제 목표: 학습자는 데이터베이스가 엑셀과 뭐가 다른지, PK/FK가 무엇이고 1:N 관계가 데이터를 어떻게 연결는지, SELECT/INSERT/UPDATE/DELETE를 언제 쓰는지 구분할 수 있어야 한다... 기능 요구사항: DB 선택(SQLite, MySQL 등 택 1), 제약조건 적용(NOT NULL 1개 이상, UNIQUE 1개 이상, FK 무결성 동작), 부모 테이블 선재 입력 규칙 준수, 핵심 쿼리 15개(조회 4, 조인 4/INNER 2 LEFT 1 필수, 집계 3/GROUP BY 필수, 서브쿼리 1, 수정삭제 2, 인덱스 및 이유 1줄 필수)"*

* **▶ 시니어 전문가의 명쾌한 해석**:
  * 단일 파일에 쿼리를 합치지 말고 **목적에 따라 물리 스크립트 3개(`1_schema.sql`, `2_data.sql`, `3_queries.sql`)로 엄격히 분리**하라는 뜻입니다.
  * 특히 외래키가 실제로 에러를 내며 동작해야 한다는 조건은, 데이터베이스 그릇이 비정상 유령 데이터 입력을 물리적으로 거부하는 **무결성 보호 장치**를 직접 테스트해 보라는 요구입니다.

## 3. 보너스 과제 및 제약사항 판독
> **[과제 원문 텍스트]**  
> *"보너스 과제: ①조인 1개를 두 방식으로 풀기(JOIN vs 서브쿼리 비교), ②데이터 정합성 깨뜨려 보기(FK 에러 발생 기록), ③미니 리포트 만들기(핵심 지표 3개 정의 및 쿼리)... 제약사항: 백엔드 프레임워크 사용 금지, 고급 기능(뷰, 프로시저, 트리거) 사용 금지, 과도한 정규화 이론 지양하고 직관적 구조 목표"*

* **▶ 시니어 전문가의 명쾌한 해석**:
  * 과제 수준을 학부 입문 필수 영역인 **CRUD와 테이블 병합 결합(JOIN)**에 정확히 집중시키려는 사려 깊은 제약입니다. 학술적 수학 이론에 빠져 테이블을 수십 개로 파편화하지 말고, **실제 매장 서비스 관점에서 쿼리가 직관적으로 잘 나오는 실용적 구조**를 짜내라는 지침입니다.

---

# 【제2편】 거대한 스켈레톤부터 세부 로직으로 파고드는 탑다운 실습 해설

프로젝트를 공백 상태에서 출발하여 완성할 때, 베테랑 강사가 머릿속에서 생각이 흘러가는 생중계 과정입니다.

## Level 1. 폴더 아키텍처 스켈레톤 (물리 산출물 격리 구조)
제출 규격에 맞춰 디렉토리를 아래처럼 분리 선언합니다.
```text
submission/
├── README.md                 # 프로젝트 대문 (열공한 주도적 도메인 선정 배경 + 로컬 환경 명세)
├── erd_diagram.png           # 매장 4개 테이블 구조 시각화 ERD 다이어그램 이미지
├── 1_schema.sql              # [DDL Layer] 테이블 정의 및 물리적 제약조건 스크립트
├── 2_data.sql                # [DML Seeding Layer] 참조 순서 철칙 준수 시딩 스크립트
├── 3_queries.sql             # [UseCase Layer] 매장 서비스 유스케이스 핵심 쿼리 15개
├── bonus_report.md           # [보너스 과제 종합 리포트] JOIN 비교, FK 에러 로그, 매장 KPI
└── evidence/                 # 로컬 DB 100% 실행 검증 로그 텍스트 파일들 모음
```

## Level 2. 도메인 ERD 스켈레톤 (생각의 흐름 생중계)
* **전문가의 머릿속 사고**: *"음, 식당에서 손님이 키오스크로 주문한다... 만약 표 1개에 [주문ID, 테이블번호, 메뉴명, 가격, 장르]를 다 넣으면 신메뉴 등록할 때 에러가 나겠군. 표를 쪼개자!"*
* **만능 공식 뼈대 매핑**: `[카테고리 서랍(1)] ───< [판매 메뉴 서랍(N)] ───< [주문 기록 서랍(N)] >─── [매장 좌석 서랍(1)]`
* 이 뼈대를 구축함으로써 외래키 선을 매개로 **1:N 관계 3개**가 수학적으로 무결하게 완결됩니다.

## Level 3. DDL 스키마 스켈레톤 (`1_schema.sql` 뼈대에서 세부 제약조건으로)
```sql
-- [대프레임 뼈대]: 외래키 활성화 및 기존 테이블 선 삭제
PRAGMA foreign_keys = ON;
DROP TABLE IF EXISTS orders; DROP TABLE IF EXISTS menus; DROP TABLE IF EXISTS store_tables; DROP TABLE IF EXISTS menu_categories;

-- [테이블 뼈대 1 선언]: menu_categories
CREATE TABLE menu_categories (
    id INTEGER PRIMARY KEY,            -- [세부 로직 1]: 식별자 PK 부여
    name TEXT NOT NULL UNIQUE          -- [세부 로직 2]: 비어있음 금지(NOT NULL) + 고유성 보장(UNIQUE)
);
```

## Level 4. DML 주입 순서 스켈레톤 (`2_data.sql` 부모에서 자식으로)
초보자가 가장 빈번히 구문 에러를 겪는 의존성 철칙 스켈레톤입니다.
```sql
-- [주입 순서 1: 참조 대상인 부모 테이블 선입력 완료]
INSERT INTO menu_categories VALUES ... (10줄);
INSERT INTO store_tables VALUES ... (12줄);

-- [주입 순서 2: 부모 ID를 참조하는 자식 테이블 후입력 완료]
INSERT INTO menus VALUES ... (15줄);
INSERT INTO orders VALUES ... (25줄);
```


# 【제3편】 직접 실습을 위한 백엔드 필수 지식 백과사전 (딥다이브)

## 1. 정규화(Normalization) 이론과 이상 현상(Anomalies)
표 하나에 정보가 쏠려 일어나는 3대 대참사를 물리적으로 막는 안전 공학입니다.
* **삽입 이상**: 출시 후 안 팔린 신메뉴가 기본키 공백으로 인해 등록 자체가 막히는 현상 차단.
* **수정 이상**: 가격 인상 시 누적 행 중 1개를 누락해 장부가 엇갈리는 장부 붕괴 차단.
* **삭제 이상**: 주문 취소 레코드를 지웠더니 매장 좌석 자산 정보까지 통째로 증발하는 비극 차단.

### 정규화 1NF ~ 3NF 핵심 도달 기준
* **제1정규형(1NF)**: 원자값의 원칙. 칸 안에 `곱창전골, 맥주`라고 쉼표로 적지 않고 레코드를 분리할 것.
* **제2정규형(2NF)**: 부분 종속 제거. 주문 표에 '메뉴 단가'를 적지 말고 `menus` 서랍으로 독립시킬 것.
* **제3정규형(3NF)**: 이행 종속 제거. 메뉴 표에 '카테고리 텍스트'를 적지 말고 `menu_categories` 서랍으로 격리할 것.

## 2. 식별자의 철학 : 기본키(PK)와 외래키(FK)
* **자연키(전화번호, 이메일)의 위험성**: 유저가 개인정보를 변경하거나 탈퇴하면 연결된 자식 데이터 수천 건이 미아(Orphan)가 됩니다. 순수 인공 숫자 ID(`id INTEGER PRIMARY KEY`)를 PK로 부여하면 정보가 바뀌어도 DB 뼈대는 절대 흔들리지 않습니다.
* **다대다(M:N) 물리 매핑 금지 철칙**: 좌석(M)과 메뉴(N)는 다대다 관계입니다. DB는 이를 직접 매핑할 수 없으므로 중간에 트랜잭션 테이블(`orders`)을 두어 **`좌석(1)──<(N)주문(N)>──(1)메뉴`** 공식으로 해소해야 합니다.

## 3. SQL 명령어 국제 표준 4대 구분 대백과사전
* **① DDL (데이터 정의어 / 목수 언어)**: 그릇 뼈대를 짓고 부수는 언어 (`CREATE`, `ALTER`, `DROP`, `TRUNCATE`)
* **② DML (데이터 조작어 / 사서 언어) ★실무 핵심**: 비즈니스 레코드를 넣고 빼는 언어 (`SELECT`, `INSERT`, `UPDATE`, `DELETE`)
* **③ DCL (데이터 제어어 / 경비원 언어)**: 창고 출입 보안키 통제 (`GRANT`, `REVOKE`)
* **④ TCL (트랜잭션 제어어 / 타임머신 언어)**: 영구 확정(`COMMIT`)과 에러 발생 시 1초 전 상태로 복구(`ROLLBACK`)

## 4. 질의어 조회의 꽃 `SELECT` 쿼리 물리 실행 순서 철칙
* **인간 작성 순서**: `SELECT -> FROM -> WHERE -> GROUP BY -> HAVING -> ORDER BY -> LIMIT`
* **★컴퓨터 물리 실행 순서**: `①FROM -> ②WHERE -> ③GROUP BY -> ④HAVING -> ⑤SELECT -> ⑥ORDER BY -> ⑦LIMIT`
*(WHERE 시점엔 SELECT의 별칭 Alias이 존재하지 않으므로 WHERE 별칭>10 쓰면 에러 발생 철칙 체득)*

## 5. 파편화된 표 결합 조인(JOIN)과 통계 분석(`GROUP BY`)
* **INNER JOIN (교집합 병합)**: 양쪽 표에 참조 식별자가 매칭되는 행만 수평 결합합니다.
* **LEFT JOIN (좌측 전체 외부 결합)**: 좌측 표는 무조건 다 꺼내고 매칭 없으면 `NULL` 처리합니다.
  * ★**차집합 탐색 공식 (`LEFT JOIN + IS NULL`)**: `menus LEFT JOIN orders` 후 `orders.id IS NULL`을 검색하시면 **'오늘 단 한 번도 안 팔린 판매 부진 메뉴'**를 0.01초 만에 솎아낼 수 있습니다.
* **`WHERE` vs `HAVING` 필터 시점**: 행 거르기 `WHERE`(그룹화 전 필터) vs 통계 거르기 `HAVING`(그룹화 후 필터)

## 6. 서브쿼리와 검색 가속기 인덱스 (`CREATE INDEX`)
* **인덱스의 본질**: 1,000만 건 표 풀 스캔(Full Scan) 부하를 방지하는 책 뒤편의 색인 좌표 트리입니다. 쓰기 작업 시 색인 책자도 갱신해야 하므로 쓰기 속도가 살짝 저하됩니다. 따라서 **조회 조건에 빈번히 쓰이며 고유성이 높은 컬럼**에 선별 부여해야 합니다.

---

# 【제4편】 완성된 실제 과제 구현물 및 증빙 산출물 완벽 해부

## 1. 4개 테이블 스키마 상세 규격표 (`1_schema.sql`)

| 테이블명 | 컬럼명 | 데이터 타입 | 물리적 제약조건 | 엔지니어링 책임 설명 |
|:---|:---|:---|:---|:---|
| **menu_categories** | `id` | INTEGER | PK | 카테고리 고유 식별 번호 |
| | `name` | TEXT | NOT NULL, UNIQUE | 카테고리명 (중복 등록 방지) |
| **store_tables** | `id` | INTEGER | PK | 매장 좌석 고유 식별 번호 |
| | `table_number` | INTEGER | NOT NULL, UNIQUE | 테이블 번호 (1번~12번 테이블) |
| | `capacity` | INTEGER | NOT NULL | 수용 가능 인원수 (2인석, 4인석 등) |
| **menus** | `id` | INTEGER | PK | 판매 상품 고유 번호 |
| | `name` | TEXT | NOT NULL | 상품명 |
| | `price` | INTEGER | NOT NULL | 판매 단가 (원) |
| | `category_id` | INTEGER | NOT NULL, FK | 소속 카테고리 참조 ID |
| **orders** | `id` | INTEGER | PK | 주문 트랜잭션 번호 |
| | `table_id` | INTEGER | NOT NULL, FK | 주문이 발생한 좌석 참조 ID |
| | `menu_id` | INTEGER | NOT NULL, FK | 주문된 상품 참조 ID |
| | `quantity` | INTEGER | NOT NULL | 주문 수량 |
| | `order_time` | DATETIME | NOT NULL | 주문 터치 일시 |
| | `status` | TEXT | NOT NULL | 조리 상태 ('COOKING', 'SERVED', 'CANCELLED') |

---

## 2. 15개 핵심 비즈니스 쿼리 유스케이스 총정리표 (`3_queries.sql`)

| 번호 | 요구 범주 | 쿼리 비즈니스 유스케이스 설명 | 핵심 SQL 절 조합 |
|:---:|:---|:---|:---|
| **01** | 기본 조회 | 2만원 이상 프리미엄 메뉴 가격순 정렬 상위 5개 조회 | `WHERE`, `ORDER BY`, `LIMIT` |
| **02** | 기본 조회 | 키오스크 검색어('전골', '철판') 포함 상품 검색 | `WHERE LIKE '%키워드%'` |
| **03** | 기본 조회 | 주방에서 실시간 조리 중('COOKING')인 주문 티켓 조회 | `WHERE =` |
| **04** | 기본 조회 | 6인석 이상의 다인석 및 단체 매장 좌석 목록 조회 | `WHERE >=` |
| **05** | 조인 (INNER) | 주방 모니터용 들어온 주문 티켓 상세 메뉴명 결합 조회 | `INNER JOIN` (3개 테이블 결합) |
| **06** | 조인 (INNER) | 주류 카테고리에 속하는 판매 상품들의 단가표 조회 | `INNER JOIN` |
| **07** | 조인 (LEFT) | 오늘 단 한 번도 안 팔린 판매 부진 메뉴 탐색 | `LEFT JOIN`, `IS NULL` |
| **08** | 조인 (LEFT) | 현재 손님이 앉아있지 않은 빈 매장 좌석(공석) 탐색 | `LEFT JOIN`, `IS NULL` |
| **09** | 집계 분석 | 카테고리별 등록된 메뉴 종류 수(`COUNT`)와 평균 단가(`AVG`) 집계 | `COUNT()`, `AVG()`, **`GROUP BY`** |
| **10** | 집계 분석 | 매장 좌석별 누적 주문 총금액(`SUM`)을 정산 집계하여 정렬 | `SUM()`, **`GROUP BY`** |
| **11** | 집계 분석 | 총 판매량 3개 이상의 베스트셀러 인기 메뉴 집계 | `SUM()`, **`GROUP BY`**, `HAVING` |
| **12** | 서브쿼리 | 매장에서 가장 비싼 최고가 메뉴가 포함된 주문 이력 조회 | `WHERE IN (SELECT...)` |
| **13** | 데이터 수정 | 조리가 끝난 18번 주문 건을 서빙 완료('SERVED')로 변경 | `UPDATE ... SET` |
| **14** | 데이터 삭제 | 손님의 실수로 터치 취소된 25번 주문 기록 영구 삭제 | `DELETE FROM ... WHERE` |
| **15** | 인덱스 생성 | 조리 상태 조건 검색 속도 향상을 위한 색인 부여 | `CREATE INDEX` + 이유 주석 |

---

## 3. 심화 보너스 과제 완수 내역 (`bonus_report.md` 및 `evidence/`)

1. **조인 1개를 두 가지 방식으로 풀기 (`bonus_01_compare_methods.txt`)**: 조리 중인 메뉴 조회 요구를 `INNER JOIN` 방식과 `IN (SELECT...)` 서브쿼리 방식으로 각각 실행하고 결과 집합이 100% 일치함을 검증했습니다.
2. **데이터 정합성 위반 실험 (`bonus_02_fk_error_test.txt`)**: 존재하지 않는 가상 좌석(`id=9999`)으로 주문 삽입 시도 시 RDBMS 엔진이 `sqlite3.IntegrityError: FOREIGN KEY constraint failed` 에러를 뿜으며 데이터 오염을 물리적으로 차단하는 로그를 박제했습니다.
3. **매장 경영 핵심 KPI 지표 3개 도출 (`bonus_03_kpi_metrics.txt`)**:
   * ① 메뉴 카테고리별 매출 기여 비중(%)
   * ② 좌석 규모별 평균 주문 정산액
   * ③ 주방 조리 병목 지표(조리 대기 티켓 비율) 계산 최종 SQL 도출 완료.

---

# 【부록】 비전공 일반인 독학 실전 가이드 & 밤샘 트러블슈팅 Q&A

### [치트키 1] 무료 도구(DBeaver) 원클릭 세팅법
1. 구글에 **'DBeaver Community Edition Download'**를 검색하여 무료 도구를 PC에 설치합니다.
2. 실행 후 좌측 상단의 **`[플러그 모양: 새 연결 생성]`** 버튼을 클릭하고 **`SQLite`**를 선택합니다.
3. 경로 지정 후 파일명을 **`table_order.db`**라고 입력하신 뒤 **[완료]** 버튼을 누르시면 준비가 종결됩니다.

### [치트키 2] 창의적 자유 주제 선정 시의 '만능 모델링 공식 도면'
> **만능 도면 공식**:  
> `[분류 테이블(1)] ───< [상품/콘텐츠 테이블(N)] ───< [거래 기록 테이블(N)] >─── [유저 테이블(1)]`  
*(해당 구조를 적용하시면 당근마켓, 영화관 예매, 쇼핑몰 등 어떠한 도메인을 가져오시더라도 평가 조건인 '테이블 4개 + 1:N 관계 2개 이상'이 100% 무결하게 성립합니다.)*

### [치트키 3] 구문 오류 발생 시 AI 트러블슈팅 프롬프트
> *"저는 현재 비전공 일반인 수준에서 SQLite 규격의 데이터베이스 실습을 진행하고 있습니다. 제가 작성한 아래의 SQL 코드를 실행하니 에러 메시지가 발생했습니다. **[수정 완료된 정상 코드]**와 함께 **[오류 원인을 일반인이 이해하기 쉬운 일상 비유로 설명]**해 주십시오."*  
> *(본 템플릿 하단에 본인 코드와 에러 텍스트를 붙여넣기 하십시오.)*


---

# 【심화 부록 2】 초보자가 밤새 헤매는 진짜 에러 트러블슈팅 베스트 20선 (1:1 Q&A)

### Q1. DBeaver에서 `.sql` 스크립트를 실행했는데 `FOREIGN KEY constraint failed` 에러가 납니다.
> **A (전문가의 답변)**: 100% **입력 순서 오류**입니다. 자식 테이블인 `orders`에 데이터를 넣으려는데 참조 대상인 `store_tables`에 해당 `table_id`가 존재하지 않기 때문입니다. 반드시 `2_data.sql` 스크립트 위쪽의 부모 테이블 `INSERT` 구문부터 실행하십시오.

### Q2. 쿼리를 쳤는데 `no such column: total_price` 라고 나옵니다.
> **A**: 컴퓨터 엔진의 실제 물리적 실행 순서 철칙 때문입니다. `WHERE` 절은 `SELECT` 절보다 먼저 실행되므로 `SELECT price*qty AS total_price`에서 만든 별칭을 `WHERE total_price > 1000`에서 참조할 수 없습니다. `WHERE price*qty > 1000`으로 원본 연산식을 그대로 적으셔야 합니다.

### Q3. `GROUP BY`를 썼더니 `SELECT` 절의 특정 컬럼 때문에 에러가 납니다.
> **A**: `GROUP BY 좌석번호`로 행들을 수직 묶기 한 뒤에는, 개별 행마다 값이 다른 '주문시간(`order_time`)' 같은 일반 속성을 `SELECT`에 단독 표기할 수 없습니다. 반드시 `MAX(order_time)`처럼 집계 함수로 축약하시거나 `GROUP BY` 기준 컬럼만 명시하셔야 합니다.

### Q4. 문자열 검색할 때 `WHERE name = '%전골%'` 이라고 쳤는데 아무것도 안 나옵니다.
> **A**: 와일드카드 퍼센트(`%`) 기호를 구사하실 땐 등호 연산자(`. =`)가 아닌 반드시 **`LIKE`** 키워드를 쓰셔야 합니다. 등호 연산자는 진짜 이름이 글자 그대로 '%전골%'인 레코드만 검색합니다.

### Q5. 데이터가 비어있는 행을 찾으려 `WHERE return_date = NULL` 이라고 쳤는데 결과가 안 나옵니다.
> **A**: SQL 국제 표준에서 공백(`NULL`)은 알 수 없는 미지의 값이므로 비교 연산자(`. =`)로 판독할 수 없습니다. 반드시 **`IS NULL`** 키워드를 구사하셔야 합니다.

*(중략: 베테랑 엔지니어의 실전 검증 노하우 완결)*
