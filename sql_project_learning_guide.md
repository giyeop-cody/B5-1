# [완전 정복 가이드] SQL로 만드는 나만의 데이터베이스

> **대상**: 컴퓨터 사이언스 전공 지식이 없고 파이썬 기초만 조금 아는 비전공자/입문자  
> **목표**: 백엔드 프레임워크 없이 스스로 관계형 데이터베이스(RDBMS)를 설계하고 비즈니스 요구사항을 SQL로 해결하는 역량 확보  
> **예상 소요 시간**: 총 40시간 (권장: 하루 4시간 × 10일 완성 로드맵)

---

## 1. 프로젝트 개요 및 초보자를 위한 전략

### 이 미션의 진짜 핵심
많은 초보자들이 데이터베이스(DB)를 **"데이터가 아주 많이 들어가는 거대한 엑셀 파일"** 정도로 오해합니다. 하지만 이 미션이 요구하는 핵심은 엑셀과 DB의 본질적 차이인 **'테이블 간의 관계(Relationship)와 규칙(Constraint)'**을 이해하는 것입니다.

* **엑셀 사고방식**: 한 면에 회원의 이름, 전화번호, 빌린 책 제목, 빌린 날짜를 중복을 감수하고 모두 적어둠. (책 이름을 바꾸려면 수백 개의 행을 일일이 수정해야 함)
* **데이터베이스 사고방식**: `회원 테이블`과 `도서 테이블`을 따로 만들고, `대여 기록 테이블`이 두 테이블의 **고유 번호(ID)**만 가리키도록 연결(JOIN)함.

### 파이썬 눈꼽만큼 아는 수준에서의 접근법
* 파이썬 문법(변수, 리스트, 간단한 조건/반복문)을 안다는 것은 **"컴퓨터가 데이터를 처리하는 논리적 흐름"**에 거부감이 없다는 큰 장점입니다.
* 이번 미션은 백엔드 코딩(Python)을 배제하고 순수하게 **SQL 언어와 데이터 구조**에만 집중합니다. 코딩 복잡성 없이 데이터 구조 설계라는 SW 개발의 가장 밑바탕을 탄탄히 다질 수 있는 최고의 기회입니다.

---

## 2. 초보자 맞춤 추천 개발 환경 (스타터 팩)

과제에서는 SQLite, MySQL, PostgreSQL, H2 중 하나를 고르라고 제시하고 있습니다. **입문자에게는 무조건 아래의 조합을 강력히 추천합니다.**

### ① 데이터베이스 엔진: **SQLite**
* **추천 이유**: MySQL이나 PostgreSQL은 컴퓨터에 '복잡한 백그라운드 서버 프로그램'을 설치하고 계정/비밀번호/포트 설정을 해야 합니다. 초보자는 여기서 하루를 날립니다. 반면 **SQLite는 서버 설치가 필요 없고, `.db` 파일 하나로 데이터베이스가 작동**하므로 복사나 제출이 아주 쉽습니다.

### ② DB 관리 도구(GUI): **DBeaver (디비버) Community Edition**
* **추천 이유**: 무료이며 한글이 지원되고, SQLite뿐만 아니라 추후 실무에서 쓸 MySQL, Postgres까지 모두 연동되는 업계 표준 툴입니다. 클릭 몇 번으로 테이블 안에 들어있는 데이터를 엑셀처럼 눈으로 확인할 수 있습니다.

### ③ 설계 시각화 툴: **dbdiagram.io** (웹 브라우저 무료 툴)
* **추천 이유**: ERD(테이블 관계도)를 마우스로 그리면 예쁘게 정렬하기 힘듭니다. 이 사이트는 왼쪽에 간단한 코드(DBML)를 적으면 오른쪽에 자동으로 프로급 ERD 다이어그램 이미지를 생성해 주어 보너스 제출물 만들기에 최적입니다.

---

## 3. 완수를 위해 꼭 필요한 핵심 지식 체계 (Step-by-Step)

미션을 성공적으로 끝내기 위해 학습해야 할 지식을 **9가지 Step**으로 정리했습니다.

---

### [Step 1] 관계형 데이터베이스(RDBMS)의 기초 개념
* **필요한 지식**:
  * 테이블(Table), 행(Row/Record), 열(Column/Field)의 개념
  * 데이터 타입: 숫자(`INTEGER`), 문자열(`TEXT` 또는 `VARCHAR`), 날짜/시간(`DATE`, `DATETIME`)
* **왜 필요한가**: 컴퓨터에게 "여기는 숫자만 들어와야 해", "여기는 날짜 기록이야"라고 엄격하게 규정을 정해주어야 데이터 오염(엑셀에 날짜 대신 '어제'라고 적는 일 등)을 막을 수 있습니다.
* **추천 검색 키워드**: *“관계형 데이터베이스 기초 개념”, “SQL 데이터 타입 종류”*

---

### [Step 2] 데이터 모델링과 식별자 (PK / FK)
* **필요한 지식**:
  * **기본키 (Primary Key, PK)**: 각 행을 유일하게 구분할 수 있는 주민등록번호 같은 주민 ID (주로 `id INTEGER` 사용)
  * **외래키 (Foreign Key, FK)**: 다른 테이블의 PK를 참조하는 연결 고리
  * **1:N 관계 (일대다 관계)**: 예를 들어 `회원 1명`은 `여러 번 책을 대여(N)`할 수 있습니다. 이때 '대여 기록' 테이블에 '회원 ID'를 적어두는 것이 1:N 설계의 핵심입니다.
* **왜 필요한가**: 과제의 필수 조건(*최소 4개 테이블, 1:N 관계 2개 이상*)을 통과하기 위한 핵심 이론입니다.
* **추천 검색 키워드**: *“데이터베이스 PK FK 설명”, “RDBMS 일대다 관계 예시”, “정규화 아주 쉽게 이해하기”*

---

### [Step 3] 테이블 구조 만들기 (DDL: CREATE TABLE)
* **필요한 지식**:
  * `CREATE TABLE 테이블명 (...)` 문법
  * **제약 조건(Constraints)**:
    * `NOT NULL`: "이 칸은 절대 비어있으면 안 됨" (예: 회원이름)
    * `UNIQUE`: "이 칸의 값은 전체에서 유일해야 함" (예: 이메일, 휴대전화 번호)
    * `PRIMARY KEY`, `FOREIGN KEY` 선언 방법
* **왜 필요한가**: 제출물 ①번인 **스키마 생성 스크립트(`.sql`)**를 직접 작성해야 합니다.
* **추천 검색 키워드**: *“SQLite CREATE TABLE 문법”, “SQL 제약조건 NOT NULL UNIQUE”*

---

### [Step 4] 데이터 밀어넣기 (DML: INSERT)와 '순서의 규칙'
* **필요한 지식**:
  * `INSERT INTO 테이블명 (컬럼1, 컬럼2) VALUES (값1, 값2);`
  * **★ 외래키 의존성에 따른 입력 순서 규칙**:
    * 자식(FK를 가진 쪽)을 만들려면 부모(PK 원본을 가진 쪽)가 먼저 존재해야 합니다.
    * *(옳은 예)*: 회원 가입(`member` 삽입) → 책 등록(`book` 삽입) → 책 대여(`rental` 삽입)
    * *(틀린 예)*: 회원이 없는데 대여 기록부터 삽입하려고 하면 DB가 에러를 뿜으며 거부합니다.
* **왜 필요한가**: 제출물 ②번인 **샘플 데이터 스크립트(테이블당 10행 이상)**를 만들 때 이 순서를 지키지 않으면 파일 실행이 중간에 멈춥니다.
* **추천 검색 키워드**: *“SQL INSERT 여러 개 동시에 넣기”, “외래키 데이터 삽입 순서 에러”*

---

### [Step 5] 데이터 조회의 기본 (SELECT & WHERE)
* **필요한 지식**:
  * `SELECT` (가져올 컬럼), `FROM` (어디서)
  * `WHERE` (조건 필터링: `=`, `>`, `<`, `LIKE '%키워드%'`, `AND`, `OR`, `IN`)
  * `ORDER BY` (정렬: `ASC` 오름차순, `DESC` 내림차순)
  * `LIMIT` (상위 N개만 자르기)
* **왜 필요한가**: 핵심 쿼리 15개 중 **'기본 조회 4개 이상'** 조건을 충족합니다. (예: "2026년에 가입한 회원 중 이름이 김씨인 사람 상위 5명 조회")
* **추천 검색 키워드**: *“SQL SELECT WHERE 기초”, “SQL LIKE 패턴 검색”*

---

### [Step 6] 쪼개진 테이블 합치기 (JOIN)
* **필요한 지식**:
  * **INNER JOIN (교집합)**: 양쪽 테이블에 모두 매칭되는 데이터만 연결해서 보여줌
  * **LEFT JOIN (왼쪽 기준 전체)**: 왼쪽 테이블은 무조건 다 보여주고, 오른쪽 테이블에 매칭되는 게 없으면 `NULL`(빈칸)로 표기해서 보여줌
* **왜 필요한가**: 핵심 쿼리 15개 중 **'조인 4개 이상(INNER 2개, LEFT 1개 필수)'** 조건을 충족합니다.
  * *실무 예시*: "한 번도 책을 빌리지 않은 유령 회원 찾기" → `member LEFT JOIN rental`을 한 뒤 `rental.id IS NULL`인 조건 검색!
* **추천 검색 키워드**: *“SQL INNER JOIN LEFT JOIN 차이 그림으로 이해하기”*

---

### [Step 7] 데이터 그룹화와 통계 (GROUP BY & 집계 함수)
* **필요한 지식**:
  * 집계 함수: `COUNT()`(개수), `SUM()`(합계), `AVG()`(평균), `MAX()`, `MIN()`
  * `GROUP BY 기준컬럼`: 특정 컬럼끼리 묶어서 통계 내기
  * `HAVING`: 그룹화된 결과에 조건 걸기 (`WHERE`는 그룹화 전 조건, `HAVING`은 그룹화 후 조건)
* **왜 필요한가**: 핵심 쿼리 15개 중 **'집계 3개 이상'** 조건 충족. (예: "장르별 대여 횟수 평균 및 총합 구하기")
* **추천 검색 키워드**: *“SQL GROUP BY HAVING 차이점”, “SQL 집계함수 기초”*

---

### [Step 8] 서브쿼리(Subquery)와 데이터 변경(UPDATE / DELETE)
* **필요한 지식**:
  * **서브쿼리**: 쿼리 안의 쿼리. `WHERE 컬럼 IN (SELECT ...)` 형태로 많이 쓰임.
  * `UPDATE 테이블명 SET 컬럼=값 WHERE 조건;` (※ WHERE 빠뜨리면 전 데이터가 바뀜에 주의!)
  * `DELETE FROM 테이블명 WHERE 조건;`
* **왜 필요한가**: 핵심 쿼리 조건 중 **'서브쿼리 1개 이상'**, **'수정 및 삭제 2개 이상'**을 만족해야 합니다.
* **추천 검색 키워드**: *“SQL WHERE 절 서브쿼리 예시”, “SQL UPDATE DELETE 주의할 점”*

---

### [Step 9] 검색 속도를 높이는 가속기 (INDEX)
* **필요한 지식**:
  * 인덱스의 개념: 책 뒤에 있는 '색인(찾아보기)' 페이지와 같음. 
  * `CREATE INDEX 인덱스명 ON 테이블명 (컬럼명);`
  * **어떤 컬럼에 걸어야 하는가?**: `WHERE` 조건으로 검색을 자주 하거나 `JOIN`의 연결고리가 되는 컬럼(예: 회원의 이메일, 책 제목 등). 중복 값이 너무 많은 컬럼(예: 성별 '남/여')에는 인덱스를 걸면 오히려 손해라는 개념 이해.
* **왜 필요한가**: 필수 조건 **'인덱스 1개 + 적용 이유 1줄 작성'** 충족.
* **추천 검색 키워드**: *“데이터베이스 인덱스 쉽게 이해하기”, “SQL 인덱스 생성 기준”*

---

## 4. 비전공자 맞춤 스마트 학습법 (공부 팁)

책을 처음부터 끝까지 읽고 시작하려면 지칩니다. **"필요한 개념을 빠르게 습득하고 바로 쿼리를 쳐보는 방식"**으로 공부해야 합니다.

### 팁 1. AI(ChatGPT / Claude)를 '개인 실습 코치'로 이용하는 프롬프트
이론을 보다가 막힐 때 AI에게 아래처럼 질문하세요. 코딩 지식이 조금 있는 비전공자에게 가장 완벽한 멘토가 됩니다.
> *"나는 비전공자이고 SQL을 막 배우고 있어. **[LEFT JOIN]**의 개념을 초등학생도 이해할 수 있게 '카페 주문 상황'에 빗대어 설명해 주고, 간단한 예시 테이블과 쿼리 코드도 보여줘."*

### 팁 2. 무료 추천 온라인 레퍼런스 및 실습 사이트
1. **W3Schools SQL Tutorial** (구글에 'w3schools sql' 검색): 웹 브라우저 위에서 바로 SQL 버튼을 누르며 문법을 테스트해 볼 수 있는 최고의 무료 영문/한글 레퍼런스.
2. **프로그래머스 SQL 고득점 Kit** (무료): `SELECT`, `GROUP BY`, `JOIN` 파트의 Level 1~2 문제들을 풀어보시면 과제에 필요한 쿼리 감각이 100% 충전됩니다.
3. **생활코딩 '관계형 데이터베이스' 혹은 'MySQL' 강의** (유튜브 무료): 비전공자 언어로 데이터베이스가 왜 필요한지 개념을 잡아주는 대한민국 최고의 입문 강의.

---

## 5. 총 40시간 완성 로드맵 (10일 실습 계획표)

하루 4시간 투자를 기준으로 한 **10일 완성 실습 스케줄**입니다. 주말을 이용해 하루 8시간씩 5일 만에 끝내실 수도 있습니다.

| 일자 | 단계 | 핵심 학습 목표 및 수행 내용 | 해당 과제 산출물 | 소요 시간 |
|:---:|:---|:---|:---|:---:|
| **Day 1** | **환경 구축 & DB 맛보기** | ① DBeaver 설치 및 SQLite 연동 테스트<br>② DB vs 엑셀 차이 유튜브 강의 시청<br>③ W3Schools에서 SELECT 기본 문법 연습 | 실습 환경 준비 완료 | 4h |
| **Day 2** | **도메인 선정 & 스키마 초안** | ① 나만의 주제 선정 (추천: **도서 대여점**)<br>② 종이 노트에 4개 테이블 네모 그리고 선 긋기 (PK, FK 지정)<br>③ 1:N 관계 2개가 제대로 성립하는지 검증 | 도메인 초안 설계기록 | 4h |
| **Day 3** | **ERD 시각화 & DDL 학습** | ① `dbdiagram.io` 접속해서 내 스키마 입력해보기<br>② `CREATE TABLE`, `NOT NULL`, `UNIQUE` 문법 학습<br>③ DBeaver에 쿼리 날려 빈 테이블 4개 생성 성공하기 | **ERD 다이어그램 이미지** | 4h |
| **Day 4** | **스키마 스크립트 작성** | ① Day 3의 테이블 생성 코드를 다듬어 `.sql` 파일 1개로 완성<br>② 테이블을 싹 지웠다가(`.sql` 실행 한 번으로) 완벽히 복구되는지 테스트 | **`1_schema.sql` 완성** | 4h |
| **Day 5** | **데이터 입력 (INSERT) 정복** | ① `INSERT INTO ... VALUES` 문법 집중 연습<br>② 부모 테이블 → 자식 테이블 순서 규칙에 따라 더미 데이터 작성<br>③ 각 테이블당 10줄 이상 데이터 입력 스크립트 완성 | **`2_data.sql` 완성** | 4h |
| **Day 6** | **기본 조회 & 집계 쿼리** | ① `WHERE`, `ORDER BY`, `LIMIT` 활용 조회 쿼리 4개 작성<br>② `COUNT`, `SUM`, `AVG`, `GROUP BY` 활용 집계 쿼리 3개 작성<br>③ DBeaver 결과창 캡처 및 1줄 설명 달기 | 쿼리 1~7번 완성 | 4h |
| **Day 7** | **조인의 꽃 (INNER & LEFT JOIN)**| ① `INNER JOIN` 쿼리 2개 작성 (예: 대여중인 책 이름과 빌린 사람 이름)<br>② `LEFT JOIN` 쿼리 2개 작성 (예: 한 번도 대여 안 된 재고 도서 찾기)<br>③ 결과 캡처 및 주석 설명 작성 | 쿼리 8~11번 완성 | 4h |
| **Day 8** | **서브쿼리 & 수정/삭제/인덱스** | ① 서브쿼리 1개, UPDATE 1개, DELETE 1개 작성<br>② `CREATE INDEX` 쿼리 1개 작성 + 주석에 **적용 이유 1줄** 작성<br>③ 총 15개 핵심 쿼리 모음집 검증 | **`3_queries.sql` 완성** | 4h |
| **Day 9** | **보너스 미션 도전** | ① 보너스 1: 조인으로 푼 문제를 서브쿼리로 바꿔 풀기<br>② 보너스 2: 없는 회원 ID로 대여 기록 넣으며 FK 에러 발생 캡처<br>③ 보너스 3: 핵심 비즈니스 KPI 지표 3개 도출 쿼리 작성 | 보너스 리포트 초안 | 4h |
| **Day 10** | **최종 검토 및 제출물 패키징** | ① 빈 SQLite DB를 새로 생성하여 `schema` → `data` → `queries` 순서로 오류 없이 원클릭 실행되는지 최종 테스트<br>② 결과 캡처 이미지들 폴더 정리 및 파일명 확인 | **최종 산출물 제출** | 4h |

---

## 6. 추천 도메인 예시 가이드 ("도서 대여 서비스")

어떤 주제로 할지 막막하시다면, 과제 가이드에 제시된 **도서 대여점**을 그대로 벤치마킹하여 시작하세요. 관계가 가장 명확해서 쿼리 15개를 뽑아내기 쉽습니다.

### 4개 테이블 구조 명세 (추천안)

1. **`category` (도서 카테고리 테이블)**
   * `id` (INTEGER, PK)
   * `name` (TEXT, NOT NULL, UNIQUE)  *(예: '소설', '과학', '역사')*

2. **`book` (도서 정보 테이블)**
   * `id` (INTEGER, PK)
   * `title` (TEXT, NOT NULL)
   * `author` (TEXT)
   * `published_date` (DATE)
   * `category_id` (INTEGER, FK → `category.id`)  **[1:N 관계 ①] 카테고리 하나에 책 여러 권**

3. **`member` (회원 테이블)**
   * `id` (INTEGER, PK)
   * `name` (TEXT, NOT NULL)
   * `phone` (TEXT, NOT NULL, UNIQUE)
   * `join_date` (DATE)

4. **`rental` (대여 기록 테이블)**
   * `id` (INTEGER, PK)
   * `member_id` (INTEGER, FK → `member.id`)  **[1:N 관계 ②] 회원 한 명이 대여 여러 번**
   * `book_id` (INTEGER, FK → `book.id`)      **[1:N 관계 ③] 책 한 권이 역대로 여러 번 대여됨**
   * `rent_date` (DATE, NOT NULL)
   * `return_date` (DATE) *(아직 반납 안 했으면 NULL)*

---

## 7. 초보자가 과제 제출 전 반드시 체크할 함정 (Checklist)

* [ ] **외래키 활성화 여부 확인 (SQLite 사용 시 주의!)**
  * SQLite는 기본적으로 프로그램 속도 때문에 외래키 검사 기능이 꺼져 있는 경우가 있습니다. DBeaver에서 스크립트 맨 위에 `PRAGMA foreign_keys = ON;` 구문을 한 줄 넣어주시면 완벽합니다.
* [ ] **데이터 삽입(`.sql`)을 실행할 때 에러가 나지 않는가?**
  * 제출한 `INSERT` 파일 전체를 복사해서 새로 만든 빈 DB에 붙여넣고 실행했을 때 단 한 줄의 에러도 없이 100% 실행되어야 감점이 없습니다.
* [ ] **주석(Comment)을 정성껏 달았는가?**
  * 15개 쿼리 위에 `-- [기본조회 1] 2026년 이후 가입한 회원 명단 조회` 처럼 채점자가 한눈에 알아보도록 설명을 적어주세요.
* [ ] **백엔드 코드를 쓰지 않았는가?**
  * Python 스크립트로 DB를 조회하거나 데이터를 넣지 마세요. 오직 순수 `.sql` 텍스트 파일 세 개만 제출해야 합니다.

---
*파이썬 지식이 조금이라도 있다는 것은 '논리 구조'에 이미 익숙하다는 뜻입니다. 자신감을 갖고 Step 1부터 찬찬히 DBeaver에 쿼리를 쳐보세요. 이 프로젝트가 끝날 때쯤이면 "왜 현업에서 데이터베이스를 쓰는지" 정확한 관점을 갖게 되실 겁니다!*
