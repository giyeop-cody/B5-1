=== Q15 [인덱스] 반복되는 status 필터의 후보 인덱스를 만들고 효과는 실행 계획으로 확인한다. ===

SQL
-- [Q15][인덱스] 반복되는 status 필터의 후보 인덱스를 만들고 효과는 실행 계획으로 확인한다.
CREATE INDEX IF NOT EXISTS idx_order_status ON orders (status);

RESULT
rows_affected
----------------------------------------
-1

VERIFICATION
idx_order_status exists PASS
query_plan=SEARCH orders USING INDEX idx_order_status (status=?)
