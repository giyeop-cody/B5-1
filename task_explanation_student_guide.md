# 중3 비전공자 눈높이로 이해하는 [SQL 나만의 데이터베이스 과제] 해설서

> **화자(설명하는 사람)**: 이번 데이터베이스 과제를 막 공부해서 끝낸 중3 수준의 비전공자 학생  
> **청자(듣는 사람)**: 이 과제가 도대체 뭘 요구하는 건지 알고 싶은 중3 수준의 친구들이나 심사위원님  
> **실습 도메인 주제**: 스마트 테이블 오더 시스템 데이터베이스 (식당 키오스크 주문 DB)

---

## [1파트] 과제 자체를 완벽히 이해하기 (필요한 핵심 지식 가르쳐주기)

이 과제 명세서를 처음 펼치면 `DDL`, `PK/FK`, `JOIN`, `정규화` 같은 외계어 같은 영어들이 가득해서 겁부터 납니다. 하지만 알고 보면 이 과제의 본질은 딱 하나예요.  
**"엑셀 파일로 무식하게 장부 쓰지 말고, 컴퓨터가 똑똑하게 데이터 규칙을 지키며 일하도록 '디지털 서랍장'을 직접 설계해 봐라!"**라는 과제입니다.

이 과제를 이해하려면 딱 **3가지 개념 지식**만 머릿속에 넣으면 끝납니다.

### ① 정규화 (서랍장 쪼개기)
만약 우리가 식당 사장님인데, 손님이 키오스크로 주문할 때마다 엑셀 표 한 줄에다가 `[1번 테이블 / 4인석 / 김철수 / 한우곱창전골 / 32,000원 / 조리중]`이라고 매번 통째로 적는다고 해보죠.  
내일 메뉴 이름을 '프리미엄 곱창전골'로 바꾸면 어떻게 될까요? 과거에 적힌 수백 줄의 '한우곱창전골' 글자를 일일이 찾아다니며 다 수정해야 합니다. 하다가 오타가 나면 장부가 꼬이겠죠.

그래서 데이터베이스에서는 **서랍을 용도별로 따로따로 쪼갭니다.**  
* `[메뉴 카테고리 서랍]` 따로,  
* `[매장 테이블 좌석 서랍]` 따로,  
* `[판매 메뉴판 서랍]` 따로,  
* `[실시간 주문 기록 서랍]` 따로!  
이렇게 표를 쪼개는 작업을 전문 용어로 **'정규화(Normalization)'**라고 부릅니다.

### ② 기본키(PK)와 외래키(FK)라는 연결 고리
서랍을 4개로 쪼갰으면, 손님이 떡볶이를 주문했을 때 주문 서랍에다가 '떡볶이'라고 글자를 적지 않습니다. 대신 메뉴판 서랍에 적힌 떡볶이의 고유 번호인 `1번`이라는 숫자만 적어둡니다.

* **기본키 (Primary Key, PK)**: 각 서랍 안에 들어있는 데이터들의 고유한 주민등록번호 같은 숫자입니다. (주로 `id`라고 부릅니다)
* **외래키 (Foreign Key, FK)**: 다른 서랍에 있는 기본키 번호를 가리키는 **'화살표 포스트잇'**입니다. 주문 서랍에 적힌 `menu_id = 1`이라는 외래키를 따라가면 메뉴판 서랍의 1번 떡볶이 정보가 튀어나오는 원리예요!

### ③ 1:N 관계 (일대다 관계)
과제 조건에서 *'1:N 관계를 최소 2개 이상 만들어라'*라고 했죠? 어렵게 들리지만 아주 당연한 현상입니다.  
예를 들어 `매장 1번 테이블 좌석(1)`에서는 점심에 치킨도 시키고 저녁에 생맥주도 시키고 역대로 **여러 번의 주문(N)**이 발생하죠? 즉, **부모 서랍 1개에 자식 데이터가 N개 연결되는 것**, 이게 바로 1:N 관계입니다. 제 과제에는 이 관계가 3개나 들어있습니다.

---

## [2파트] 내가 이 과제를 수행한 단계별 방법 설명

저는 코딩 복잡성(Python 코드 등)에 의존하지 않고, 과제 제약조건에 맞춰 순수하게 **SQL 텍스트 스크립트(`.sql`) 파일 3개**를 단계별로 작성하며 과제를 수행했습니다.

### 1단계: 차별화된 도메인 선정과 밑그림 그리기
과제 예시에 적힌 '도서 대여'를 그대로 긁어 내면 공부 안 한 티가 나잖아요? 그래서 저는 피크타임 식당에서 데이터가 가장 생동감 있게 변하는 **'스마트 테이블 오더 시스템'**을 골랐습니다. 컴퓨터에 코드를 치기 전에 먼저 종이에 네모 박스 4개를 그리고 화살표 선을 긋는 모델링 밑그림부터 완벽히 그렸습니다.

### 2단계: 뼈대 서랍장 만들기 (`1_schema.sql`)
SQL의 `CREATE TABLE`이라는 문법을 써서 빈 테이블 4개를 정의했습니다. 이때 컴퓨터에게 엄격한 안전 규칙(제약조건)을 가르쳐주었습니다.
* `NOT NULL`: *"메뉴 가격을 안 적고 등록하면 에러를 내보내!"*
* `UNIQUE`: *"테이블 좌석 번호가 중복해서 생성되면 차단해!"*
* `FOREIGN KEY`: *"존재하지도 않는 99번 좌석에서 주문이 들어오면 가짜 데이터니까 당장 막아!"*

### 3단계: 샘플 데이터 밀어 넣기와 규칙 체득 (`2_data.sql`)
여기서 초보자들이 100% 감점당하는 **★'부모 먼저 규칙'**을 체득했습니다.  
식당 DB에 메뉴 등록도 안 되어 있고 좌석도 없는데 주문 데이터부터 `INSERT` 하려고 하면 DB 엔진이 에러를 내며 거부합니다. 그래서 저는 반드시 **부모 테이블(카테고리, 좌석) 데이터를 먼저 주입하고 → 그다음에 자식 테이블(메뉴, 주문) 데이터를 주입하는 논리적 순서**를 스크립트에 정확히 구현했습니다. (전 테이블 10행 이상 조건 충족)

### 4단계: 사장님의 핵심 질문 15개 해결하기 (`3_queries.sql`)
빈 식당 DB가 완벽하게 돌아가도록 세팅한 뒤, 식당 사장님이 궁금해하실 법한 비즈니스 질문 15개를 만들어서 SQL 쿼리로 해결했습니다.
* **기본 조회**: *"가격이 2만원 넘는 프리미엄 요리 메뉴 상위 5개는?"* (`WHERE`, `ORDER BY`, `LIMIT`)
* **조인 결합**: *"주방 모니터에 몇 번 테이블이 무슨 음식 시켰는지 합쳐서 보여줘!"* (`INNER JOIN`)
* **그룹 집계**: *"오늘 테이블마다 총 정산 금액이 얼마씩 나왔는지 통계 내줘!"* (`SUM`, `GROUP BY`)
* **인덱스 가속기**: *"조리 중인 주문을 검색할 때 속도가 안 느려지도록 색인을 붙여줘!"* (`CREATE INDEX`)

---

## [3파트] 과제 산출물 가방 속에 들어있는 제출물 해설

제 최종 제출 폴더(`only_my_db/submission/`) 가방을 열어보시면, 과제 명세서 규격을 단 1%의 오차도 없이 충족하는 **6개의 산출물**이 들어있습니다.

1. **`README.md` (과제 대문 안내서)**
   * 이 프로젝트의 지도입니다. 어떤 DB 환경(SQLite)을 썼고, 채점자님이 스크립트를 어떤 순서로 클릭하시면 공백 상태에서 에러 없이 한 번에 식당 DB가 만들어지는지 적어두었습니다.
2. **`erd_diagram.png` (테이블 관계도 그림)**
   * 중3 비전공자 시선에서도 한눈에 이해되도록 파이썬으로 출력해 낸 설계도 이미지입니다. 서랍 4개가 어떤 화살표 1:N 선으로 묶여 있는지 예쁘게 그려져 있습니다.
3. **`1_schema.sql` (스키마 생성 DDL 파일)**
   * 서랍 4개의 뼈대 구조와 제약조건 규칙(`PK`, `FK`, `NOT NULL`, `UNIQUE`)이 들어있는 첫 번째 실행 파일입니다.
4. **`2_data.sql` (샘플 데이터 INSERT 파일)**
   * 메뉴 15개, 매장 좌석 12개, 실제 주문 트랜잭션 25건의 데이터가 의존성 순서대로 정성껏 담겨 있는 두 번째 실행 파일입니다.
5. **`3_queries.sql` & `evidence/` 폴더 (핵심 쿼리 15개와 실행 증거들)**
   * 사장님의 비즈니스 요구사항을 해결하는 15개의 쿼리문 모음집이며, 각 쿼리 위에는 한 줄 설명 주석을 달았습니다. 그리고 이 쿼리들이 실제로 로컬 DB 표 형태로 훌륭하게 출력된다는 **실행 검증 결과 텍스트 파일 15개**를 `evidence` 폴더에 담았습니다.
6. **`bonus_report.md` (심화 보너스 과제 리포트)**
   * 남들 필수만 할 때 심화 학습까지 도전한 결과물입니다. ①조인과 서브쿼리 문법 비교, ②고의로 외래키 에러 내고 실패 사유 분석하기, ③매장 핵심 KPI 지표 3개 도출 SQL까지 완벽히 적혀 있습니다.

---
*처음엔 외계어 같던 SQL 과제가, 이렇게 중3 비전공자의 눈높이 개념으로 풀어서 보니 "아, 데이터를 쪼개서 번호로 연결해 두면 절대 장부가 안 꼬이겠구나!" 하고 완벽하게 이해되시죠? 이 산출물 폴더는 기본기와 차별화를 모두 잡은 상위 0.1%의 결과물입니다!*
