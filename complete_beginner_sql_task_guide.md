# [비전공자 완전 정복] SQL 나만의 데이터베이스 과제 A to Z 해설서

> **대상 눈높이**: 컴퓨터 사이언스 전공 지식이 없고 SQL을 막 처음 접하는 중학교 3학년 수준의 비전공자  
> **목적**: 과제가 요구하는 본질을 A부터 Z까지 차근차근 이해하고, 스스로 실습할 수 있는 최소한의 필수 핵심 지식 체득  
> **실습 도메인 주제**: 스마트 테이블 오더 시스템 데이터베이스 (식당 키오스크 주문 DB)

---

## 제1장. 이 과제의 진짜 정체는 뭔가요? (엑셀과의 결정적 차이)

이 과제 명세서를 처음 펴면 영어 코드와 외계어 같은 전문 용어가 가득해서 겁부 납니다. 하지만 알고 보면 이 과제의 진짜 정체는 딱 하나예요.  
**"식당이나 카페에서 손님 데이터를 '엑셀 파일 한 장'에 무식하게 적지 말고, 컴퓨터가 안전하고 똑똑하게 일할 수 있는 [디지털 서랍장(데이터베이스)] 구조로 직접 짜봐라!"**라는 실습 과제입니다.

### 왜 사장님들은 그냥 엑셀 표 하나에 다 안 적을까요?

우리가 식당 사장님이고, 손님이 키오스크로 주문할 때마다 엑셀 표 한 줄에다가 아래처럼 적는다고 해보죠.
* `[1번 테이블 / 4인석 / 김철수 / 한우곱창전골 / 32,000원 / 조리중]`
* `[1번 테이블 / 4인석 / 김철수 / 생맥주 / 5,000원 / 서빙완료]`

만약 내일에 메뉴 이름을 '프리미엄 한우곱창전골'로 바꾸면 어떻게 될까요? 과거에 적힌 수백 줄의 글자를 일일이 찾아다니며 고쳐야 합니다. 고치다가 실수로 숫자를 잘못 입력하면 식당 전체 매출 장부가 통째로 꼬이겠죠.

그래서 진짜 개발자들은 **'데이터베이스(DB)'**라는 기술을 씁니다.  
거대한 표 하나에 다 때려 박는 게 아니라, **[메뉴판 서랍] 따로, [매장 좌석 서랍] 따로 만들어 두고 주문이 들어올 때 각 서랍의 고유 번호(포스트잇 번호)만 화살표로 딱딱 연결해서 똑똑하게 관리하는 방식**입니다. 이걸 전문 용어로 *'테이블 간 관계를 형성한다'*고 하며, 이번 과제의 핵심 목표입니다.

---

## 제2장. 과제를 풀기 위해 꼭 알아야 하는 최소한의 지식 4가지

이 과제를 직접 이해하고 스스로 쿼리를 칠 수 있으려면 딱 **4가지 핵심 개념 지식**만 머릿속에 넣으면 끝납니다.

### 지식 1. 정규화 (서랍을 용도별로 나누기)
표 하나에 정보가 쏠려 중복이 생기지 않도록 표를 쪼개는 작업입니다. 우리 과제에서는 식당 운영을 위해 표를 딱 4개로 나눴습니다.
1. `menu_categories` (메뉴 분류 서랍: 메인요리, 주류 등)
2. `store_tables` (매장 좌석 서랍: 1번 테이블, 2번 테이블 등)
3. `menus` (판매 메뉴 서랍: 곱창전골, 쭈꾸미 등)
4. `orders` (실시간 주문 기록 서랍: 손님이 키오스크 터치할 때마다 쌓이는 곳)

### 지식 2. 기본키(PK)와 외래키(FK)라는 연결 고리
서랍을 4개로 나눴으면, 손님이 떡볶이를 시켰을 때 주문 기록 서랍에 '떡볶이'라고 글자를 텍스트로 적지 않습니다. 대신 메뉴판 서랍에 있는 떡볶이의 고유 번호인 `1번` 숫자만 적습니다.

* **기본키 (Primary Key, PK)**: 각 서랍 안에 들어있는 데이터 행들의 고유한 주민등록번호 같은 숫자입니다. (보통 `id`라고 씁니다)
* **외래키 (Foreign Key, FK)**: 다른 서랍에 있는 PK 번호를 가리키는 **'화살표 포스트잇'**입니다. 주문 서랍에 적힌 `menu_id = 1`이라는 외래키를 따라가면 메뉴판 서랍의 1번 떡볶이 가격과 이름이 튀어나오는 원리예요!

### 지식 3. 1:N 관계 (일대다 관계)
과제 조건에서 *'1:N 관계를 최소 2개 이상 만들어라'*라고 했죠? 아주 당연한 자연 현상입니다.  
예를 들어 `매장 1번 테이블 좌석(1)`에서는 점심에 떡볶이도 시키고 저녁에 생맥주도 시키고 역대로 **여러 번의 주문(N)**이 발생하죠? 즉, **부모 서랍 1개에 자식 데이터가 N개 연결되는 현상**, 이게 바로 1:N 관계입니다.

### 지식 4. ★부모 먼저 규칙 (입력 순서 철칙)
초보자들이 100% 에러를 내고 감점당하는 부분입니다.  
식당 DB에 메뉴판 등록도 안 되어 있고 매장 좌석도 없는데, 다짜고짜 주문 데이터부터 넣으려고 하면 컴퓨터가 *"등록되지 않은 가짜 음식 번호야!"* 하면서 에러를 냅니다.  
따라서 반드시 **부모 테이블(카테고리, 좌석) 데이터를 먼저 넣고 → 그다음에 자식 테이블(메뉴, 주문) 데이터를 넣는 순서 규칙**을 지켜야 합니다.

---

## 제3장. 사장님의 질문을 해결하는 SQL 명령어 기초 문법

식당 DB에 가짜 손님 데이터를 넣고 나면, 사장님이 궁금해하실 법한 질문 15개를 해결해야 합니다. 이때 쓰는 SQL 명령어의 원리입니다.

### ① 기본 조회 (`SELECT`, `WHERE`, `ORDER BY`, `LIMIT`)
* `SELECT 이름, 가격 FROM menus WHERE 가격 >= 20000 ORDER BY 가격 DESC LIMIT 5;`
* **원리**: `menus` 서랍에서(`FROM`), 가격이 2만원 넘는 것만 걸러서(`WHERE`), 비싼 순서대로 정렬한 뒤(`ORDER BY ... DESC`), 딱 상위 5개만 잘라서 보여줘(`LIMIT 5`)!

### ② 흩어진 서랍 깡총 묶기 (`INNER JOIN`, `LEFT JOIN`)
주문 기록 서랍에는 메뉴 번호(`menu_id = 3`)만 적혀있습니다. 화면에 숫자 대신 '감자전'이라는 진짜 이름을 보여주고 싶을 때 씁니다.
* **INNER JOIN (양쪽 다 있는 교집합)**: 주문 내역과 메뉴판을 공통 ID 기준으로 옆으로 병합해서 보여줌.
* **LEFT JOIN (왼쪽 기준 전체)**: 왼쪽 메뉴판은 무조건 다 보여주고, 오른쪽에 주문된 내역이 없으면 빈칸(`NULL`)으로 표기함. 
  * *마법의 활용법*: `menus LEFT JOIN orders`를 한 뒤 주문 ID가 비어있는(`IS NULL`) 조건을 걸면 **'단 한 번도 안 팔린 악성 재고 메뉴'**를 찾아낼 수 있습니다!

### ③ 통계 내기 (`SUM`, `AVG`, `COUNT`, `GROUP BY`)
* **GROUP BY**: 특정 컬럼(예: 매장 좌석 ID)끼리 행들을 묶어주는 기능입니다.
* 좌석별로 행을 묶은 뒤 `SUM(단가*수량)`을 쓰면 **테이블별 총 정산 매출액**이 단 한 줄로 계산됩니다.

### ④ 수정 및 삭제 (`UPDATE`, `DELETE`)
* 조리가 끝난 음식을 서빙 완료 상태로 바꿀 때: `UPDATE orders SET status = 'SERVED' WHERE id = 18;`
* *(※ 주의: `WHERE` 조건을 빼먹으면 식당 전체 주문이 전부 서빙 완료로 바뀝니다!)*

---

## 제4장. 최종 과제 산출물 폴더 속에 든 파일들의 역할

제출 가방(`only_my_db/submission/`)을 여시면 아래의 파일들이 협동하여 식당 DB를 완성합니다.

```text
submission/
├── README.md                 # 1. 안내서: 이 DB를 어떻게 컴퓨터에서 실행하는지 적힌 대문
├── erd_diagram.png           # 2. 청사진: 테이블 4개가 화살표로 어떻게 묶여있는지 보여주는 그림
│
├── 1_schema.sql              # 3. 빈 서랍장 짜기 코드 (DDL 규칙 선언)
├── 2_data.sql                # 4. 메뉴 15개와 실제 주문 25건을 순서대로 넣는 코드 (DML)
├── 3_queries.sql             # 5. 사장님의 비즈니스 질문 15개를 해결하는 마법의 쿼리문
│
├── bonus_report.md           # 6. 보너스 심화 학습(조인 비교, 에러 실험, 외식업 KPI 도출) 보고서
└── evidence/                 # 7. 증거 사진첩: 15개 쿼리가 진짜 표로 출력된다는 결과 텍스트 파일들
```

---

### 자습 및 실습 팁
이 문서를 읽고 개념이 잡히셨다면, 무료 DB GUI 도구인 **DBeaver**를 열고 `새 빈 SQLite 연결`을 만드신 뒤 `1_schema.sql` → `2_data.sql` → `3_queries.sql` 순서로 텍스트를 복사해 실행 버튼을 눌러보세요. 

*아하! 데이터를 표 하나에 안 적고 쪼개서 ID 번호로 묶어두면 데이터가 절대 안 꼬이고 0.1초 만에 분석되는구나!* 하고 100% 체득하게 되실 겁니다!
